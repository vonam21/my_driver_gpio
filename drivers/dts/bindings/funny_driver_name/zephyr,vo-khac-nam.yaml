description: |
    An example sensor that reads the GPIO level defined in input-gpios.

compatible: "zephyr,vo-khac-nam"
properties:
    gpios:
        type: phandle-array
        required: true
        description: |
            Input gpios.
include: base.yaml